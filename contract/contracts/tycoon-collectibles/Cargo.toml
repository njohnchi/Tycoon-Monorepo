[package]
name = "tycoon-collectibles"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
tycoon-lib = { path = "../tycoon-lib" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
